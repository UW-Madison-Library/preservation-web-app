=begin
#Preservation Manager Public API

#Public API for the Preservation Manager.

OpenAPI spec version: 1.0.0

Generated by: https://github.com/swagger-api/swagger-codegen.git
Swagger Codegen version: 3.0.32
=end

require 'date'

module PreservationManager
  class FileDiff
    attr_accessor :path

    # The sha256 digest of the file in the batch
    attr_accessor :new_sha256_digest

    # The sha256 digest of the file in the preserved object
    attr_accessor :old_sha256_digest

    # The relationship between the batch object file and the preserved object file
    attr_accessor :diff

    attr_accessor :format_diff

    attr_accessor :encoding_diff

    attr_accessor :validity_diff

    class EnumAttributeValidator
      attr_reader :datatype
      attr_reader :allowable_values

      def initialize(datatype, allowable_values)
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.include?(value)
      end
    end

    # Attribute mapping from ruby-style variable name to JSON key.
    def self.attribute_map
      {
        :'path' => :'path',
        :'new_sha256_digest' => :'newSha256Digest',
        :'old_sha256_digest' => :'oldSha256Digest',
        :'diff' => :'diff',
        :'format_diff' => :'formatDiff',
        :'encoding_diff' => :'encodingDiff',
        :'validity_diff' => :'validityDiff'
      }
    end

    # Attribute type mapping.
    def self.openapi_types
      {
        :'path' => :'Object',
        :'new_sha256_digest' => :'Object',
        :'old_sha256_digest' => :'Object',
        :'diff' => :'Object',
        :'format_diff' => :'Object',
        :'encoding_diff' => :'Object',
        :'validity_diff' => :'Object'
      }
    end

    # List of attributes with nullable: true
    def self.openapi_nullable
      Set.new([
      ])
    end
  
    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(attributes = {})
      if (!attributes.is_a?(Hash))
        fail ArgumentError, "The input argument (attributes) must be a hash in `PreservationManager::FileDiff` initialize method"
      end

      # check to see if the attribute exists and convert string to symbol for hash key
      attributes = attributes.each_with_object({}) { |(k, v), h|
        if (!self.class.attribute_map.key?(k.to_sym))
          fail ArgumentError, "`#{k}` is not a valid attribute in `PreservationManager::FileDiff`. Please check the name to make sure it's valid. List of attributes: " + self.class.attribute_map.keys.inspect
        end
        h[k.to_sym] = v
      }

      if attributes.key?(:'path')
        self.path = attributes[:'path']
      end

      if attributes.key?(:'new_sha256_digest')
        self.new_sha256_digest = attributes[:'new_sha256_digest']
      end

      if attributes.key?(:'old_sha256_digest')
        self.old_sha256_digest = attributes[:'old_sha256_digest']
      end

      if attributes.key?(:'diff')
        self.diff = attributes[:'diff']
      end

      if attributes.key?(:'format_diff')
        self.format_diff = attributes[:'format_diff']
      end

      if attributes.key?(:'encoding_diff')
        self.encoding_diff = attributes[:'encoding_diff']
      end

      if attributes.key?(:'validity_diff')
        self.validity_diff = attributes[:'validity_diff']
      end
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array.new
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      diff_validator = EnumAttributeValidator.new('Object', ['ADDED', 'REMOVED', 'MODIFIED', 'UNCHANGED'])
      return false unless diff_validator.valid?(@diff)
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] diff Object to be assigned
    def diff=(diff)
      validator = EnumAttributeValidator.new('Object', ['ADDED', 'REMOVED', 'MODIFIED', 'UNCHANGED'])
      unless validator.valid?(diff)
        fail ArgumentError, "invalid value for \"diff\", must be one of #{validator.allowable_values}."
      end
      @diff = diff
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          path == o.path &&
          new_sha256_digest == o.new_sha256_digest &&
          old_sha256_digest == o.old_sha256_digest &&
          diff == o.diff &&
          format_diff == o.format_diff &&
          encoding_diff == o.encoding_diff &&
          validity_diff == o.validity_diff
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [path, new_sha256_digest, old_sha256_digest, diff, format_diff, encoding_diff, validity_diff].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        elsif attributes[self.class.attribute_map[key]].nil? && self.class.openapi_nullable.include?(key)
          self.send("#{key}=", nil)
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :DateTime
        DateTime.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        {}.tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        PreservationManager.const_get(type).build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {}
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.include?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        {}.tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end  end
end
